// PostCSS is a tool that transforms CSS. Tailwind is a PostCSS plugin —
// it reads your HTML/JSX files, finds the Tailwind classes you used,
// and generates the actual CSS rules for them.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
